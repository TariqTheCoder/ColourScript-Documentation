[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "ColourScript"
authors = [{name = "Tariq Tayebi", email = "tariq.tayebi1@education.nsw.gov.au"}]
dynamic = ["version", "description"]
